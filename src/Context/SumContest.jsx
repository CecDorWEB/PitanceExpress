// import { createContext, useReducer } from "react";

// const initialState = {
//   stateQuantity: [],
// };

// const SumContext = createContext();
// const SumProvider = ({ children }) => {
//   const [total, setTotal] = useReducer(sum, initialState);

//   return (
//     <SumContext.Provider value={{ total, setTotal }}>
//       {children}
//     </SumContext.Provider>
//   );
// };

// export default { SumContext, SumProvider };
